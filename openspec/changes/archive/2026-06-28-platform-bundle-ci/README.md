# platform-bundle-ci

Cross-platform packaging and release CI: adds macOS (.dmg/.app) bundle target and a GitHub Actions build matrix (ubuntu-22.04 + macos-latest runners). Windows deferred. Unsigned for now; Apple notarization is a human-gated follow-up.
