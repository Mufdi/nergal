# platform-proc

Abstract all /proc-based process and port inspection behind a cross-platform platform_proc module so process-tree walks, process cwd, and listening-TCP-port discovery work on Linux and macOS behind one interface
