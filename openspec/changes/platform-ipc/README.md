# platform-ipc

Cross-platform IPC transport abstraction (PlatformListener/PlatformStream) over unix-socket vs windows-named-pipe; macOS-first verify + trait seam, Windows deferred. Unify blocking plan-review/ask-user FIFOs onto the same primitive.
